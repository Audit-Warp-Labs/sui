# ⚙️ Sui Configuration Files

These templates provide localnet/testnet settings to support AuditWarp CLI scripts.

## 🌐 Networks

- `default.sui-config.yaml` — Use as base configuration.
- Customize as needed per deployment target.
